[package]
name = "loti-tui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
loti-core.workspace = true
anyhow.workspace = true
# Only the core seam names a stamp type; it stays wrapped so no other module has
# to know what a stamp is.
jiff.workspace = true
ratatui.workspace = true
ratatui-markdown.workspace = true
crossterm.workspace = true
# The external editor is handed a real file, because that is the only interface
# every editor has; the file is removed when the round-trip ends, whatever the
# editor did.
tempfile.workspace = true
